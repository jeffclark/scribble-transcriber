{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the application",
  "windows": ["main"],
  "permissions": [
    "dialog:default",
    "shell:default",
    "core:default",
    "core:path:default",
    "http:default"
  ]
}
