# Installing Scribble

Install instructions have moved to **[docs/INSTALL_UNSIGNED.md](docs/INSTALL_UNSIGNED.md)**.

That doc covers:
- Downloading the `.dmg` from GitHub Releases
- The Gatekeeper workaround for **macOS 14 Sonoma and earlier** (right-click → Open)
- The different Gatekeeper workaround for **macOS 15 Sequoia and later** (System Settings → Privacy & Security → Open Anyway)
- Troubleshooting and uninstall
